(executable
  (name run) (modules Run)
  (libraries unix codept_lib precomputed)
)

(executable
  (name serialization) (modules Serialization)
  (libraries unix codept_lib precomputed)
)


(alias
 (name   runtest)
 (action (run %{exe:run.exe} ../../.. ))
 )

(alias
 (name   runtest)
 (action (run %{exe:serialization.exe}))
)